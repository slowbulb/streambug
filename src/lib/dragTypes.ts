// Custom dataTransfer MIME type used to identify an in-page drag of a single
// track version (as opposed to a whole track, or files dragged in from the
// OS) — shared between the draggable version row (TrackRow) and the global
// drop target that turns it into a new track (VersionDragZone).
export const VERSION_DRAG_MIME = "application/x-streambug-version";
